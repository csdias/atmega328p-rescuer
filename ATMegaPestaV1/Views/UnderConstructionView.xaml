<UserControl x:Class="ATMegaPestaV1.Views.UnderConstructionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="&#x1F6A7;"
                       FontSize="48"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16"/>
            <!-- These two were #CDD6F4 and #A6ADC8, Catppuccin Mocha text colours from a
                 dark theme that never existed: 1.33:1 and 2.04:1 over the #F5F5F5
                 background, against the 4.5:1 text needs. This screen is reachable from
                 two menu entries, so it was two menu entries that looked blank. -->
            <TextBlock x:Name="TxtTitle"
                       Foreground="{DynamicResource BrushTextTitle}" FontSize="22" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Este módulo encontra-se em desenvolvimento."
                       Foreground="{DynamicResource BrushTextNormal}" FontSize="14"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
